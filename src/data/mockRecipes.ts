// Fichier vide - les recettes sont maintenant gérées directement via Supabase
import { Recipe } from '../types';

// Tableau vide pour éviter les erreurs d'import
const mockRecipes: Recipe[] = [];

export default mockRecipes;